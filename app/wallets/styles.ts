import styled from "styled-components";

export const WallestWrapper = styled("div")({
  display: "flex",
  flexDirection: "column",
  gap: "30px",
  width: "100%",
});

export const FormWrapper = styled("div")({
  display: "flex",
  minHeight: "416px",
});
