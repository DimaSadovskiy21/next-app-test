import styled from "styled-components";

export const HeaderWrapper = styled('header')({
    display: 'flex',
    justifyContent: 'flex-end',
    alignItems: 'flex-end',
    minHeight: '124px',
    padding: "14px 0",
})