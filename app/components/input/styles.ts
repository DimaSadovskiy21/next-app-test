'use client';
import styled from "styled-components";

export const CustomInput = styled("input")({
  minWidth: "439px",
  padding: '7px 12px',
  border: "none",
  borderRadius: "5px",
  fontFamily: "inherit",
  fontWeight: "inherit",
  fontSize: "inherit",
  outline: "none",
});
